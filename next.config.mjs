/** @type {import('next').NextConfig} */
const nextConfig = {
  // Hides the on-screen Next.js dev indicator (the floating logo button shown
  // only during `npm run dev`). It never appears in a production build anyway.
  devIndicators: false,
};

export default nextConfig;
